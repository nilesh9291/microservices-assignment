group = 'com.springboot'
version = '0.0.1-SNAPSHOT'

description = """app-rest-client"""

apply plugin: 'application'
mainClassName = 'com.resttemplate.RestClientApplication'

dependencies {
    compile project(':app-commons')
}
