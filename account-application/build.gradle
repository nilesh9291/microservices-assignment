group = 'com.springboot'
version = '0.0.1-SNAPSHOT'

description = """account-application"""

apply plugin: 'application'
mainClassName = 'com.rest.accountapp.AccountApplication'

dependencies {
    compile project(':app-commons')
}
